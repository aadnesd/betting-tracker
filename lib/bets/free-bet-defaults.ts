export const DEFAULT_WIN_WAGERING_MULTIPLIER = 1;
export const DEFAULT_WIN_WAGERING_MIN_ODDS = 1.5;
export const DEFAULT_WIN_WAGERING_EXPIRES_IN_DAYS = 30;

// Default number of days a free bet stays usable before it expires.
// For locked promos (with unlock/wagering requirements) the window starts
// when the qualifying bet that completes the requirement settles, not at
// creation time.
export const DEFAULT_FREE_BET_EXPIRY_DAYS = 7;
